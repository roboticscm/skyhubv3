import 'package:flutter/material.dart';

class QrCodeTabContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("QrCode content"),
    );
  }
}
